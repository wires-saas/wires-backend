export enum SupportedEmailsProvider {
  Mailjet = 'mailjet',
}

export enum SenderStatus {
  Available = 'available',
  Unavailable = 'unavailable',
}

export enum DomainStatus {
  Verified = 'verified',
  Pending = 'pending',
}
